# DESIGN.md — 로열빌더스클럽

> AI 코딩 에이전트가 이 프로젝트의 화면을 만들 때 반드시 따르는 시각 규칙.
> Google Stitch 표준 규격(색상·타이포그래피·간격·표면·컴포넌트·모션·레이아웃·응답동작·가드레일)을 따른다.
> 컨셉: **"Trust Ledger — 신뢰의 대장(臺帳)"**. 오래된 법무법인·은행의 서류와 장부에서 느껴지는
> 조용한 권위. 화려하게 말하지 않고, 기록으로 증명하는 디자인.

---

## 1. 원칙 (왜 이 디자인인가)

1. **기록의 미학** — 이 사이트는 브로셔가 아니라 대장(장부)처럼 보여야 한다.
   정보 밀도가 높고, 선과 번호로 정리되어 있으며, 빈 공간은 사치가 아니라 여백이다.
2. **절제된 색** — 딥네이비(#1F4066)와 종이색(#F7F5F1)이 화면의 90%. 브라스(#C9A96A)는
   한 화면에 2~3곳 이하에서만 쓴다. 금색을 많이 쓰면 프리미엄이 아니라 전단지가 된다.
3. **세리프가 신뢰를 말한다** — 큰 제목은 명조 계열 세리프. 본문만 프리텐다드 고딕.
   제목까지 고딕이면 "어디서 본 랜딩페이지"가 된다.
4. **각이 곧 태도** — 모서리 각진(최대 4px) 사각형만 사용. 둥근 카드, 둥근 배지는 금지.

## 2. 색상

| 역할 | 값 | 사용 |
|---|---|---|
| primary (네이비) | #1F4066 | 단색 면, 굵은 강조 |
| navy-deep | #14304E 계열 | 다크 섹션 배경 |
| brass | #C9A96A | 킥커, 소제목, 포인트 라인, 별점 — 장식 전용 |
| background (종이) | #F7F5F1 | 기본 배경 |
| card | #FFFFFF | 카드/패널 |
| border | #E5E1D8 | 헤어라인. 카드 구분은 그림자 대신 이 선으로 |
| text | #18181B / muted #55606E | 본문 |

- 그라데이션 텍스트·네온·유리(글래스모피즘) 전면 금지.
- 이미지 위 오버레이는 단색 네이비 알파값만 허용.

## 3. 타이포그래피

- **전체 프리텐다드(Pretendard Variable) 단일 서체** — 사용자 결정(2026-09-21).
  세리프 실험은 종료. 제목은 굵기(800~900)와 크기로 위계를 만든다.
  H1 54px/900 · H2 34~38px/900 · 자간 -0.02em · 행간 1.2~1.3.
- 본문/라벨: 400~800, 13.5~15px, 행간 1.7~1.85.
- **킥커**(섹션 위 영문 소제목): 11~13px, 자간 0.18~0.22em, 대문자, 브라스.
- 숫자는 `tabular-nums`. 실적 숫자는 대형 900 웨이트로.

## 4. 레이아웃

- 컨테이너 `max-w-7xl`, 좌우 여백 24px. **PC 우선** — 그리드는 2~4분할.
- **균등 3~4분할 아이콘 카드 그리드 금지** (가장 대표적인 AI 티). 대신:
  - 번호 매긴 **대장 행(ledger row)**: 번호 | 제목 | 설명 | 화살표, 헤어라인으로 구분
  - 비대칭 2분할 (5:7, 4:8)
  - 표/리스트 밀도
- 섹션 리듬: 모든 섹션을 같은 간격·같은 구조로 반복하지 않는다.
  다크/라이트/종이를 교차하고, 한 섹션은 통단(풀블리드 밴드)으로 끊는다.
- 정렬은 좌측 기본. 중앙 정렬은 배너성 한 줄 인용에만.

## 5. 표면과 컴포넌트

- 카드: `bg-card` + `border 1px #E5E1D8` + radius 0~4px. 그림자 기본 없음,
  호버 시 `border-brass/60`으로만 반응 (위로 뜨는 -translate-y 금지).
- 버튼: 각진 직사각형. 주 버튼 네이비 배경, 강조 CTA는 브라스 배경+네이비 글자.
  그라데이션·라운드풀 버튼 금지.
- 구분: 섹션 사이는 `border-t/b` 헤어라인. 굵은 구분선은 브라스 20~40% 알파만.
- 표: 얇은 가로선만. 세로선 없음. 헤더 배경 없음, 굵은 글자만.

## 6. 모션

- 스크롤 등장: 페이드+24px 상향 이동, 600~700ms, ease-out. **콘텐츠는 기본 표시 상태**(프로그레시브 인핸스먼트).
- 숫자 카운트업 허용(실적). 기관명 마퀴: 40~75초 선형, 호버 시 정지.
- 호버 트랜지션 150~300ms. 통통 튀는 스케일·바운스 이징 금지.

## 7. 이미지

- 실제 사진(현장·사무실·서류). 일러스트·3D 렌더·아이콘 남용 금지.
- 사진 처리: 네이비 단색 오버레이(알파 45~70%) 또는 무보정 그대로.
  듀오톤·과채도 필터 금지.

## 8. 가드레일 — 이것만은 하지 말 것

1. 중앙 정렬 히어로 + 그라데이션 제목 ❌
2. "아이콘 + 제목 + 설명" 똑같은 카드가 3~4개 나란히 ❌ → 대장 행 또는 비대칭 레이아웃으로
3. 이모지, 과도한 배지/칩 ❌
4. 모든 섹션에 애니메이션 ❌ (절반만)
5. 가격표 3열 카드 ❌ → 가격을 숨기고 문의 유도
6. 둥근 모서리(8px 이상) ❌
7. "혁신적인", "새로운", "최고의" 같은 공허한 수식어 ❌ → 숫자와 절차로 말하기

## 9. 콘텐츠 톤

- 단어 **"컨설팅" 금지** → 전문가, 전문 자문, 실무 담당자.
- 존댓말 단문. 문장 끝에 약속을 둔다 ("반려 없이, 한 번에").
- 근거 없는 최고급 표현 대신 구체적 절차·숫자·출처를 제시.

### 한국어 카피 원칙 (한국어 웹 카피라이팅 조사 기반)

1. **고객의 언어** — 고객이 속으로 하는 말 그대로. ("반려될 서류는, 내기 전에 압니다")
2. **헤드라인은 고객의 가치** — 회사 슬로건·추상 명사 나열 금지. 짧게(10자 안팎 호흡).
3. **동사 중심·짧은 문장** — "제공합니다"보다 "맡깁니다/끝냅니다". 한 문단 3문장 이하.
4. **번역투·AI 티 제거** — "~를 통해", "~에 있어", 기계적 대구, 형용사 쌓기 금지.
5. **형용사 대신 숫자와 장면** — "최고의" 대신 "17년 / 1,400건 / 영업일 하루".
6. **CTA는 부담 없이** — "가입 신청"보다 "지금 상황 물어보기". 선택지는 최대 2개.
7. **리듬** — 읽었을 때 입에 붙는지 소리 내어 확인할 것.
